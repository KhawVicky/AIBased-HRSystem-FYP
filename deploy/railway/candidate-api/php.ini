upload_max_filesize=10M
post_max_size=32M
memory_limit=256M
; Candidate submission persists the application/resume first; the separate
; CLI worker owns the longer parser/scoring execution.
max_execution_time=420
